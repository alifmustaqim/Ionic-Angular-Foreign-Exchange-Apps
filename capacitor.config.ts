import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'foreign_exchange',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
